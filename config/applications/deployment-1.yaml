apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-1
  annotations:
    "chtc.wisc.edu/personal-ap": "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-1
  template:
    metadata:
      labels:
        app: nginx-1
    spec:
      containers:
      - name: nginx
        image: nginx:latest
